console.log("Loading DB Module");

import dotenv from "dotenv";
dotenv.config();

import pkg from "pg";
import { exec } from "child_process";
import path from "path";
import fs from "fs";

const { Pool } = pkg;

/* ============================
   PostgreSQL Configuration
   ============================ */

export const dbConfig = {
  host: process.env.DB_HOST || "localhost",
  port: Number(process.env.DB_PORT || 5432),
  user: process.env.DB_USER,
  password: process.env.DB_PW,
  database: process.env.DB_NAME,
  ssl: process.env.DB_SSL === "true" ? { rejectUnauthorized: false } : false,
};

/* ============================
   Database Connection Wrapper
   ============================ */

export class DBConn {
  constructor(config = dbConfig) {
    console.log("Establishing PostgreSQL DB Connection");
    this.pool = new Pool(config);
  }

  async query(sql, values) {
    try {
      const result = await this.pool.query(sql, values);
      return result.rows;
    } catch (err) {
      console.error("PostgreSQL Query Error:", err.message);
      throw err;
    }
  }

  async testConnection() {
    try {
      await this.pool.query("SELECT 1");
      console.log("✅ PostgreSQL database connected successfully.");
      return true;
    } catch (error) {
      console.error("❌ PostgreSQL connection test failed:", error.message);
      return false;
    }
  }

  async close() {
    await this.pool.end();
  }
}

export const db = new DBConn();

/* ============================
   Python Dependency Check
   ============================ */

export function checkPythonDependencies() {
  // Default Python command by OS
  let pythonPath = process.platform === "win32" ? "python" : "python3";

  // Prefer local virtual environment if it exists
  const localVenvPython = path.resolve(
    process.cwd(),
    "venv",
    process.platform === "win32" ? "Scripts/python.exe" : "bin/python3",
  );

  if (fs.existsSync(localVenvPython)) {
    pythonPath = localVenvPython;
  }

  return new Promise((resolve, reject) => {
    exec(
      `"${pythonPath}" -m pip show psycopg2-binary openai`,
      (error, stdout, stderr) => {
        if (error) {
          console.error("❌ Python package check error:", error.message);
          return reject(error);
        }

        // pip writes this warning to stderr, not stdout
        if (stderr && /Package\(s\) not found/i.test(stderr)) {
          console.error("❌ Python stderr:", stderr.trim());
          return reject(new Error(stderr));
        }

        console.log("🐍 Python packages installed:\n", stdout);
        return resolve(true);
      },
    );
  });
}

/* ============================
   Optional Startup Helper
   ============================ */

export async function startServer(app, port) {
  const isDbConnected = await db.testConnection();
  if (!isDbConnected) {
    console.error("❌ Database not connected. Aborting server startup.");
    process.exit(1);
  }

  try {
    await checkPythonDependencies();
  } catch {
    console.error("❌ Python dependencies missing. Aborting server startup.");
    process.exit(1);
  }

  app.listen(port, () => {
    console.log(`🚀 Server running on http://localhost:${port}`);
  });
}
